name: flutter_persist_data_locally
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.5
  sembast: ^3.0.0+4
  path_provider: ^2.0.1
#  encrypt: ^4.1.0
#  crypto: ^2.1.5
  sqflite: ^2.0.0+3
  moor_flutter: ^4.0.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  moor_generator: ^4.2.1
  build_runner: ^1.12.2

flutter:
  uses-material-design: true


